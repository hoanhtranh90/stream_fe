import React from 'react';
import { View } from 'react-native';

const LoginForm = ({ login, processing, errorMessage, resetErr }) => {
    return (
        <View>

        </View>
    )
}
export default LoginForm;